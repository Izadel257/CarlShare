import React from "react"
import "./login.css"
function Login(){
    return (
        <div id = "log">
            <button id="credentials" /** We need to handle clicks here to make sure that everything goes in the right way */> 
                Login with Your Carleton Credentials
            </button>
        </div>
    )
}

export default Login